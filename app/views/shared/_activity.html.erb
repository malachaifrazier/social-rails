<% if activity_resources_exist?(activity) %>
  <div id="activity-<%= activity.id %>" class="activity panel panel-primary">
    <div class="panel-body">
      <%= render_activity(activity) %>
    </div>
  </div>
<% end %>
